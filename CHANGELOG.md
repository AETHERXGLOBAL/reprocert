# Changelog

All notable public project changes are recorded here.

The project is pre-1.0; interfaces may evolve while the public alpha is hardened.

## Unreleased

### Added

- independent public repository under AETHER X GLOBAL;
- claim execution and evidence adjudication engine;
- CLI commands: `run`, `verify`, `inspect`, `diff`;
- reusable composite GitHub Action;
- open claim and certificate schemas;
- cross-platform CI;
- reference producer-attestation workflow;
- threat model, governance, security, support, and contribution guidance.

## 0.1.0a1 — Initial public-alpha package identity

Package identity used by the initial public source baseline. A package-version identifier does not by itself imply publication to a package registry or a GitHub Release.
